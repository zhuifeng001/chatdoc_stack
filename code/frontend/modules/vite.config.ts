import { defineNuxtModule, addVitePlugin } from '@nuxt/kit'

export default defineNuxtModule({
  meta: {
    name: 'custom-module'
  },
  defaults: {},
  setup(options) {
    // console.log('addVitePlugin :>> ')
  }
})
